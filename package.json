{
  "scripts": {
    "build": "npm install --prefix apps/portal && npm --prefix apps/portal run build",
    "test:rules": "node ./scripts/test-firestore-rules.js"
  },
  "dependencies": {
    "firebase": "^12.6.0",
    "firebase-admin": "^13.6.0",
    "@google/genai": "^1.31.0",
    "@anthropic-ai/sdk": "^0.71.1"
  },
  "optionalDependencies": {},
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.3",
    "vercel": "^49.1.2"
  }
  ,
  "engines": {
    "node": "20.x"
  }
}
